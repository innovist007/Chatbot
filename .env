# ============================================================
# BASE DEFAULTS — no secrets here, safe to commit
# Secrets go in .env.local (dev) or .env.production (prod)
# ============================================================

# Google Cloud / Vertex AI agent
GCP_PROJECT_ID=innovist-master-data
GCP_LOCATION=global
AGENT_ID=agent_4c5c920f-4e17-492a-9b0b-4ae9b266f8c9
BILLING_PROJECT_ID=innovist-master-data

# Server
APP_HOST=0.0.0.0
LOG_LEVEL=info

# JWT
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=24

# Domain restriction
ALLOWED_EMAIL_DOMAIN=onestolabs.com

# Email OTP defaults
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_FROM_NAME="Innovist Analytics"
OTP_LENGTH=6
OTP_EXPIRE_MINUTES=10
OTP_MAX_ATTEMPTS=5

# Redis defaults (overridden per environment below)
REDIS_DB=0
REDIS_PASSWORD=
